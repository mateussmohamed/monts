generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  email     String @unique
  firstName String
  id        Int    @default(autoincrement()) @id
  lastName  String
  password  String
}
