generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url = "***"
}

model User {
  email     String @unique
  firstName String
  id        Int    @default(autoincrement()) @id
  lastName  String
  password  String
}
